// These are the starting points for the install UI
export enum StartingPoint {
  OVERVIEW = 'overview',
  STORAGE_CLUSTER = 'storage-cluster',
  OBJECT_STORAGE = 'object-storage',
  EXTERNAL_SYSTEMS = 'external-systems',
}
